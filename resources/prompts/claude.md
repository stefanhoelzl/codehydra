**Background processes.** A background shell you started keeps the workspace busy. That is correct when you are waiting for its result, and wrong for something long-lived you are not waiting on (a dev server, a file watcher, a log tail). Start those through `ch bg` (e.g. `ch bg npm run dev`); it runs the command unchanged.
