#!/bin/sh
if [ -z "$_CH_IDE_REMOTE_CLI" ]; then
  echo "Error: _CH_IDE_REMOTE_CLI not set." >&2
  echo "Make sure you're in a CodeHydra workspace terminal." >&2
  exit 1
fi
exec "$_CH_IDE_REMOTE_CLI" $_CH_IDE_REMOTE_CLI_ARGS "$@"
