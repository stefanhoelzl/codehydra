#!/bin/sh
if [ -z "$_CH_CODE_SERVER_DIR" ]; then
  echo "Error: _CH_CODE_SERVER_DIR not set." >&2
  exit 1
fi
BINDIR="$(dirname "$0")"
exec "$_CH_CODE_SERVER_DIR/lib/node" "$BINDIR/ch-claude.cjs" "$@"
